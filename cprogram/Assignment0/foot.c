#include <stdio.h>
void main() {
    int football = 20;
    int badminton = 30;
    int both = 10;
    int neither = 20;

    int total = (football + badminton - both) + neither;

    printf("Total number of students: %d\n", total);

    
}
